# Migrations — Department 08

No new entities or schema changes. Read-only aggregation over existing entities (Property, ConstructionProject, Contact, MarketplaceListing).

## Rollback note
No migration to reverse. Revert code via Version History.