# Known Issues — Department 08

- **No time-series**: trends require historical snapshots (not captured yet).
- **Client-side aggregation**: ≤300 records per entity; large datasets need server-side aggregation.
- **No export**: CSV/PDF not implemented.
- **No saved dashboards**: no custom widget configuration.
- **Lighthouse/a11y**: scoring requires published app runtime.
- **Automated tests**: no CLI runner; e2e deferred to Testing Agent.

No critical or high-severity defects.